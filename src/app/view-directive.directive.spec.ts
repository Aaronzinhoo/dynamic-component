import { ViewDirectiveDirective } from './view-directive.directive';

describe('ViewDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
